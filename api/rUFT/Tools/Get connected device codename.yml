info:
  name: Get connected device codename
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/devices"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:6767

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
