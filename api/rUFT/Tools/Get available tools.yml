info:
  name: Get available tools
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/available"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:6767

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
