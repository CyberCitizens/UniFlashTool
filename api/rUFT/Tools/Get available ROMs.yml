info:
  name: Get available ROMs
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/available/rom"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:6767

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
