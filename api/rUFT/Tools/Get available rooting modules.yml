info:
  name: Get available rooting modules
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/available/stealth"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:6767

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
